{
  "name": "morningprint",
  "version": "0.1.0",
  "private": true,
  "description": "One original print, every morning: a receipt printer, Claude, and whatever the day brings.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.js",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:print": "node test-print.mjs",
    "status": "clasp status",
    "push": "npm run build && clasp push",
    "pull": "clasp pull"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.11",
    "esbuild": "^0.28.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
